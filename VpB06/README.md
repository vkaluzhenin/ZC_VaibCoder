# Telegram Бот для хранения задач

Минимально работающий Telegram-бот для хранения задач, используя Python, aiogram и SQLite3. Бот поддерживает удобный интерфейс с кнопками команд.

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
copy .env.example .env
```

3. Откройте `.env` и вставьте ваш токен бота от BotFather:
```
API_TOKEN=ваш_токен_бота_здесь
```

## Запуск

```bash
python bot.py
```

## Интерфейс

Бот использует удобную клавиатуру с кнопками команд, которая появляется после команды `/start`:

- **➕ Добавить задачу** — интерактивное добавление новой задачи (статус "Новый", категория "Неважная" по умолчанию)
- **📋 Список задач** — показать все задачи с их статусом и категорией
- **✏️ Редактировать задачу** — выбрать задачу для изменения статуса и категории (интерактивное меню с кнопками)
- **📥 Экспорт CSV** — получить задачи в формате CSV файла

## Команды бота

Все команды можно также использовать через текстовые команды:

- `/start` — приветственное сообщение и справка по командам (показывает клавиатуру)
- `/add <текст задачи>` — добавить новую задачу (по умолчанию: статус "Новый", категория "Неважная")
- `/list` — показать все задачи с их статусом и категорией
- `/edit` — выбрать задачу для изменения статуса и категории (интерактивное меню с кнопками)
- `/list_csv` — получить задачи в формате CSV файла

## Статусы и категории

**Статусы:**
- 🆕 **Новый** — устанавливается автоматически при создании задачи
- ✅ **Выполнена** — устанавливается вручную через команду `/edit`

**Категории:**
- 🔴 **Важная** — важная задача
- ⚪ **Неважная** — неважная задача (устанавливается по умолчанию)

## Использование

### Добавление задачи

1. Нажмите кнопку **"➕ Добавить задачу"** или используйте команду `/add <текст задачи>`
2. Если используете кнопку, введите текст задачи
3. Задача будет добавлена со статусом "Новый" и категорией "Неважная"

### Редактирование задач

1. Нажмите кнопку **"✏️ Редактировать задачу"** или используйте команду `/edit`
2. Выберите задачу из списка (inline-кнопки)
3. Нажмите кнопку для изменения статуса (Новый/Выполнена) или категории (Важная/Неважная)
4. Изменения применяются мгновенно

## Структура проекта

- `bot.py` — основной файл с кодом бота
- `tasks.db` — база данных SQLite (создается автоматически)
- `.env` — файл с переменными окружения (токен бота)

## Примечания

- База данных `tasks.db` создается автоматически при первом запуске
- При первом запуске с обновленной схемой БД существующие задачи автоматически получат статус "Новый" и категорию "Неважная"
- CSV файлы создаются временно и удаляются после отправки пользователю
- Каждый пользователь видит только свои задачи
- Статус и категория отображаются в списке задач и в CSV файле

